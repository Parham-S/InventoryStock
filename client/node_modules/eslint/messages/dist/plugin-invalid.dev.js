"use strict";

module.exports = function (it) {
  var configName = it.configName,
      importerName = it.importerName;
  return "\n\"".concat(configName, "\" is invalid syntax for a config specifier.\n\n* If your intention is to extend from a configuration exported from the plugin, add the configuration name after a slash: e.g. \"").concat(configName, "/myConfig\".\n* If this is the name of a shareable config instead of a plugin, remove the \"plugin:\" prefix: i.e. \"").concat(configName.slice("plugin:".length), "\".\n\n\"").concat(configName, "\" was referenced from the config file in \"").concat(importerName, "\".\n\nIf you still can't figure out the problem, please stop by https://eslint.org/chat/help to chat with the team.\n").trimLeft();
};