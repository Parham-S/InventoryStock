"use strict";

module.exports = function (it) {
  var configName = it.configName,
      importerName = it.importerName;
  return "\nESLint couldn't find the config \"".concat(configName, "\" to extend from. Please check that the name of the config is correct.\n\nThe config \"").concat(configName, "\" was referenced from the config file in \"").concat(importerName, "\".\n\nIf you still have problems, please stop by https://eslint.org/chat/help to chat with the team.\n").trimLeft();
};