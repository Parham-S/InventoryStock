"use strict";

module.exports = function (it) {
  var pattern = it.pattern,
      globDisabled = it.globDisabled;
  return "\nNo files matching the pattern \"".concat(pattern, "\"").concat(globDisabled ? " (with disabling globs)" : "", " were found.\nPlease check for typing mistakes in the pattern.\n").trimLeft();
};