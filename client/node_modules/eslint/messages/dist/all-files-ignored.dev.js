"use strict";

module.exports = function (it) {
  var pattern = it.pattern;
  return "\nYou are linting \"".concat(pattern, "\", but all of the files matching the glob pattern \"").concat(pattern, "\" are ignored.\n\nIf you don't want to lint these files, remove the pattern \"").concat(pattern, "\" from the list of arguments passed to ESLint.\n\nIf you do want to lint these files, try the following solutions:\n\n* Check your .eslintignore file, or the eslintIgnore property in package.json, to ensure that the files are not configured to be ignored.\n* Explicitly list the files from this glob that you'd like to lint on the command-line, rather than providing a glob as an argument.\n").trimLeft();
};