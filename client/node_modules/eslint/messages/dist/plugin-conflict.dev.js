"use strict";

module.exports = function (it) {
  var pluginId = it.pluginId,
      plugins = it.plugins;
  var result = "ESLint couldn't determine the plugin \"".concat(pluginId, "\" uniquely.\n");
  var _iteratorNormalCompletion = true;
  var _didIteratorError = false;
  var _iteratorError = undefined;

  try {
    for (var _iterator = plugins[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
      var _step$value = _step.value,
          filePath = _step$value.filePath,
          importerName = _step$value.importerName;
      result += "\n- ".concat(filePath, " (loaded in \"").concat(importerName, "\")");
    }
  } catch (err) {
    _didIteratorError = true;
    _iteratorError = err;
  } finally {
    try {
      if (!_iteratorNormalCompletion && _iterator["return"] != null) {
        _iterator["return"]();
      }
    } finally {
      if (_didIteratorError) {
        throw _iteratorError;
      }
    }
  }

  result += "\n\nPlease remove the \"plugins\" setting from either config or remove either plugin installation.\n\nIf you still can't figure out the problem, please stop by https://eslint.org/chat/help to chat with the team.\n";
  return result;
};