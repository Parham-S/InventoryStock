"use strict";

module.exports = function (it) {
  var pluginName = it.pluginName,
      resolvePluginsRelativeTo = it.resolvePluginsRelativeTo,
      importerName = it.importerName;
  return "\nESLint couldn't find the plugin \"".concat(pluginName, "\".\n\n(The package \"").concat(pluginName, "\" was not found when loaded as a Node module from the directory \"").concat(resolvePluginsRelativeTo, "\".)\n\nIt's likely that the plugin isn't installed correctly. Try reinstalling by running the following:\n\n    npm install ").concat(pluginName, "@latest --save-dev\n\nThe plugin \"").concat(pluginName, "\" was referenced from the config file in \"").concat(importerName, "\".\n\nIf you still can't figure out the problem, please stop by https://eslint.org/chat/help to chat with the team.\n").trimLeft();
};