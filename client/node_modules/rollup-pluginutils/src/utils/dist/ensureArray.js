"use strict";
exports.__esModule = true;
function ensureArray(thing) {
    if (Array.isArray(thing))
        return thing;
    if (thing == undefined)
        return [];
    return [thing];
}
exports["default"] = ensureArray;
