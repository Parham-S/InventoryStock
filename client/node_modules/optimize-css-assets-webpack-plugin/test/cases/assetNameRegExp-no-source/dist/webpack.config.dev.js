"use strict";

var _extractTextWebpackPlugin = _interopRequireDefault(require("extract-text-webpack-plugin"));

var _src = _interopRequireDefault(require("../../../src/"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

module.exports = {
  entry: './index',
  module: {
    rules: [{
      test: /\.css$/,
      use: _extractTextWebpackPlugin["default"].extract({
        fallback: {
          loader: 'style-loader'
        },
        use: {
          loader: 'css-loader'
        }
      })
    }]
  },
  plugins: [new _extractTextWebpackPlugin["default"]('file.css'), new _src["default"]({
    assetNameRegExp: /optimize-me\.css/g
  })]
};