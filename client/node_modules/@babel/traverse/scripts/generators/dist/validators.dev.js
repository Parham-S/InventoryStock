"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = generateValidators;

var _types = _interopRequireDefault(require("@babel/types"));

var _virtualTypes = _interopRequireDefault(require("../../lib/path/lib/virtual-types.js"));

var _index = _interopRequireDefault(require("@babel/types/lib/definitions/index.js"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance"); }

function _iterableToArray(iter) { if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } }

function generateValidators() {
  var output = "/*\n * This file is auto-generated! Do not modify it directly.\n * To re-generate run 'make build'\n */\nimport * as t from \"@babel/types\";\nimport NodePath from \"../index\";\nimport type { VirtualTypeAliases } from \"./virtual-types\";\n\nexport interface NodePathValidators {\n";
  var _iteratorNormalCompletion = true;
  var _didIteratorError = false;
  var _iteratorError = undefined;

  try {
    for (var _iterator = _toConsumableArray(_types["default"].TYPES).sort()[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
      var type = _step.value;
      output += "is".concat(type, "(opts?: object): this is NodePath<t.").concat(type, ">;");
    }
  } catch (err) {
    _didIteratorError = true;
    _iteratorError = err;
  } finally {
    try {
      if (!_iteratorNormalCompletion && _iterator["return"] != null) {
        _iterator["return"]();
      }
    } finally {
      if (_didIteratorError) {
        throw _iteratorError;
      }
    }
  }

  for (var _i = 0, _Object$keys = Object.keys(_virtualTypes["default"]); _i < _Object$keys.length; _i++) {
    var _type = _Object$keys[_i];
    var types = _virtualTypes["default"][_type].types;
    if (_type[0] === "_") continue;

    if (_index["default"].NODE_FIELDS[_type] || _index["default"].FLIPPED_ALIAS_KEYS[_type]) {
      output += "is".concat(_type, "(opts?: object): this is NodePath<t.").concat(_type, ">;");
    } else if (types
    /* in VirtualTypeAliases */
    ) {
        output += "is".concat(_type, "(opts?: object): this is NodePath<VirtualTypeAliases[\"").concat(_type, "\"]>;");
      } else {
      // if it don't have types, then VirtualTypeAliases[type] is t.Node
      // which TS marked as always true
      // eg. if (path.isBlockScope()) return;
      //     path resolved to `never` here
      // so we have to return boolean instead of this is NodePath<t.Node> here
      output += "is".concat(_type, "(opts?: object): boolean;");
    }
  }

  output += "\n}\n";
  return output;
}