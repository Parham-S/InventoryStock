"use strict";
/*
  Copyright 2018 Google LLC

  Use of this source code is governed by an MIT-style
  license that can be found in the LICENSE file or at
  https://opensource.org/licenses/MIT.
*/
exports.__esModule = true;
exports.normalizeHandler = void 0;
var assert_js_1 = require("workbox-core/_private/assert.js");
require("../_version.js");
/**
 * @param {function()|Object} handler Either a function, or an object with a
 * 'handle' method.
 * @return {Object} An object with a handle method.
 *
 * @private
 */
exports.normalizeHandler = function (handler) {
    if (handler && typeof handler === 'object') {
        if (process.env.NODE_ENV !== 'production') {
            assert_js_1.assert.hasMethod(handler, 'handle', {
                moduleName: 'workbox-routing',
                className: 'Route',
                funcName: 'constructor',
                paramName: 'handler'
            });
        }
        return handler;
    }
    else {
        if (process.env.NODE_ENV !== 'production') {
            assert_js_1.assert.isType(handler, 'function', {
                moduleName: 'workbox-routing',
                className: 'Route',
                funcName: 'constructor',
                paramName: 'handler'
            });
        }
        return { handle: handler };
    }
};
